module.exports = {
  root: "./", // the root directory of your project
  base: "./", // the base path for your project
  publicDir: "public", // the directory to serve as the public directory
  build: {
    outDir: "dist", // the output directory for the build
  },
};
